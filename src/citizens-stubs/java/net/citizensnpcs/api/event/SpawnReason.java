package net.citizensnpcs.api.event;

/** Compile-time mirror of Citizens 2.0.43 spawn reasons used by AIlex. */
public enum SpawnReason {
    CHUNK_LOAD,
    COMMAND,
    CREATE,
    PLUGIN,
    RESPAWN,
    TIMED_RESPAWN
}
